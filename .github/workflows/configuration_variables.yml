name: Testing configuration variables

on:
  workflow_dispatch

env:
  cloud: Microsoft Azure

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello to Anup"
        run: echo "${Greeting} ${first_name} is using $cloud
